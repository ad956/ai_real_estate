name: ai_in_real_estate
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  cached_network_image: ^3.4.1
  connectivity_plus: ^7.0.0
  dio: ^5.9.0
  fl_chart: ^0.68.0
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.1
  fluttertoast: ^9.0.0
  google_fonts: ^6.3.1
  shared_preferences: ^2.5.3
  sizer: ^3.1.3
  url_launcher: ^6.3.2

  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
